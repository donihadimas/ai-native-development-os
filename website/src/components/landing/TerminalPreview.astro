---
---

<div class="terminal-card card" aria-label="AIOS workflow terminal preview">
  <div class="terminal-header">
    <span class="dot red"></span>
    <span class="dot yellow"></span>
    <span class="dot green"></span>
    <span class="terminal-title">~/my-project</span>
  </div>
  <div class="terminal-body">
    <pre><code><span class="prompt">$</span> aios next

<span class="label">context:</span> .aios/config.json exists
<span class="label">docs:</span>   docs/product/prd.md ready
<span class="label">tasks:</span>  docs/tasks/ has 3 open tasks

<span class="success">→</span> Recommended: <span class="accent">T2 — Implement auth module</span>

<span class="prompt">$</span> cat .aios/config.json
&#123;
  <span class="key">"mode"</span>: <span class="str">"full"</span>,
  <span class="key">"docsRoot"</span>: <span class="str">"docs"</span>,
  <span class="key">"skillDelivery"</span>: <span class="str">"native"</span>
&#125;

<span class="prompt">$</span> ls docs/tasks/
T1-implement-schema.md
T2-implement-auth.md
T3-add-tests.md</code></pre>
  </div>
</div>

<style>
  .card {
    background: linear-gradient(180deg, var(--color-card-start), var(--color-card-end));
    border: 1px solid var(--color-border);
    border-radius: 0.75rem;
    overflow: hidden;
    max-width: 100%;
  }
  .terminal-card {
    box-shadow: 0 16px 32px var(--color-shadow);
    min-width: 0;
  }
  .terminal-header {
    background: rgba(0, 0, 0, 0.15);
    padding: 0.625rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    border-bottom: 1px solid var(--color-border);
  }
  .dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
  }
  .red { background: #ef4444; }
  .yellow { background: #eab308; }
  .green { background: #22c55e; }
  .terminal-title {
    margin-left: auto;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
    font-size: 0.7rem;
    color: var(--color-text-muted);
    opacity: 0.7;
  }
  .terminal-body {
    padding: 1.25rem;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
    font-size: 0.8rem;
    line-height: 1.6;
    color: var(--color-text-soft);
    overflow-x: auto;
    max-width: 100%;
  }
  pre {
    margin: 0;
  }
  .prompt {
    color: var(--color-accent);
    font-weight: 700;
  }
  .success {
    color: var(--color-accent);
  }
  .accent {
    color: var(--color-secondary);
    font-weight: 600;
  }
  .label {
    color: var(--color-text-muted);
    font-weight: 600;
  }
  .key {
    color: var(--color-secondary);
  }
  .str {
    color: var(--color-accent);
  }
  @media (max-width: 768px) {
    .terminal-body {
      padding: 1rem;
      font-size: 0.7rem;
    }
  }
</style>
