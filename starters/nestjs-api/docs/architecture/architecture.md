# Architecture

Use the architecture template from AI-Native Development OS. Include module boundaries, controllers, services, validation, auth, persistence, testing, and deployment constraints when known.
