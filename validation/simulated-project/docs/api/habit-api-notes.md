# Habit API Notes

V1 validation uses notes instead of full OpenAPI.

## Endpoints

- `POST /habits` creates a habit.
- `GET /habits` lists habits with streak data.
- `POST /habits/{id}/complete` completes a habit for the current server date.
